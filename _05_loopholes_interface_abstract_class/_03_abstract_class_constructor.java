package _05_loopholes_interface_abstract_class;

public class _03_abstract_class_constructor {
/*
 * any way we cannot create object for abstract class directly or indirectly, but abstract
 * class can contain constructor what is the need?
 * --> the main objective of abstract class constructor is to perform initialization
 * for the instance variables which are inheriting from abstract class to child 
 * class. whenever we are creating child class object automatically abstract class
 * constructor will be executed to perform initialization for the instance variables
 * which are inherited for abstract class(code reusability).   
 */
}
