package _03_decalaration_and_access_modifiers._03_member_level_modifier;

public class _04_private_members {
/**
 * 1.if a member declared as private then we can access that member only within the class,
 *  that is outside the class we cannot access.
 *  - abstract methods should be available to the child classes to provide implementation whereas
 *    private methods are not available to child class.
 *    Hence private abstract combination is illegal for methods
 */
}
